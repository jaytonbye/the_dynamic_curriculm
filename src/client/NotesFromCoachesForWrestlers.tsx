import React from "react";

export default function NotesFromCoachesForWrestlers() {
  const [notes, setNotes] = React.useState();

  //need to write the backend api calls first, then we can fetch

  return (
    <>
      <h1>
        This will be the page where wrestlers get their notes from their coaches
      </h1>
    </>
  );
}
